<Window x:Class="ChatForFun_Client.View.AddFriendWindow"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ChatForFun_Client.View"
      mc:Ignorable="d" 
      d:DesignHeight="200" d:DesignWidth="300"
      Title="AddFriend" MinWidth="300" MinHeight="200" MaxWidth="300" MaxHeight="200">

    <StackPanel Background="#FF1A1A1A">
        <TextBlock Text="Add a friend" Foreground="White" FontSize="20" HorizontalAlignment="Center" Margin="0,10,0,0" />
        <TextBox x:Name="ResponseTextBox" Margin="10,20,10,0" Height="20" />
        <Grid Margin="0,60,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Ok" Click="onClickOk" Margin="15,0" />
            <Button Grid.Column="1" Content="Cancel" Click="onClickCancel" Margin="15,0" />
        </Grid>
    </StackPanel>
</Window>
